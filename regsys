<?php
$pw=$_POST['pw'];
$em=$_POST['em'];
$lg=$_POST['lg'];
$pw1=$_POST['pw1'];
$link=mysqli_connect('localhost','Tim','AdminPass','auth');
if($link->connect_error){echo "bad";}
else{echo "very good";};

if ($pw==$pw1) {}
else{die("You entered not equal password")};
$preg_password="/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[%\$#@&\*\^\|\/~\{\}\[\]\\\])[A-Za-z\d%\$#@&\*\^\|\/~\{\}\[\]\\\]{8,}$/";
$preg_login="/^([A-Za-z]{1})([A-Za-z\d\_]{5,})$/";
if(preg_match($preg_password,$pw)){}
	else{die("You entered not wron form of password");};

if(filter_var($em, FILTER_VALIDATE_EMAIL)){}
		else{die("You entered not wron form of email");};

if(preg_match($preg_login,$login)){}
	else{die("You entered not wron form of login");};

$query1 = "INSERT into `auth1` (password, email,login) 
VALUES (md5('$pw'),'$em');"; 
$result=mysqli_query($link,$query1); 
$link->close();
?>
<html>
 <head>
<meta charset="UTF-8">
</head>
<body>
<p>You succesfully registred!</p>
<p><a href="authoriz.php">Go to authorization</a></p>
</body>
</html>
